# Credenciais do projeto Supabase (botão "Connect" > React/Vite no dashboard do Supabase).
# Deixe ambas VAZIAS para o painel rodar em "modo demo" (login aceita qualquer credencial).
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
